<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lemon.community.mapper.UserMapper">
    <resultMap id="userEntityMap" type="com.lemon.community.bean.User">
        <id column="id" property="id"></id>
        <result column="activation_code" property="activationCode"></result>
        <result column="header_url" property="headerUrl"></result>
        <result column="create_time" property="createdTime"></result>
    </resultMap>
    <sql id="selectFields">
        id, username, password, salt, email, type, status, activation_code, header_url, create_time
    </sql>
    <sql id="insertFields">
        username, password, salt, email, type, status, activation_code, header_url, create_time
    </sql>
    <select id="selectUserById" resultMap="userEntityMap">
        SELECT <include refid="selectFields"></include>
        FROM user
        WHERE id = #{id}
    </select>
    <select id="selectUserByUsername" resultMap="userEntityMap">
        SELECT <include refid="selectFields"></include>
        FROM user
        WHERE username = #{username}
    </select>
    <select id="selectUserByEmail" resultMap="userEntityMap">
        SELECT <include refid="selectFields"></include>
        FROM user
        WHERE email = #{email}
    </select>
    <insert id="insertUser" parameterType="User">
        INSERT INTO user
        (<include refid="insertFields"></include>)
        values(#{username}, #{password}, #{salt}, #{email}, #{type}, #{status}, #{activationCode}, #{headerUrl}, #{createdTime})
    </insert>
    <update id="updateStatus">
        UPDATE user SET status = #{status}
        WHERE id = #{id}
    </update>
</mapper>
